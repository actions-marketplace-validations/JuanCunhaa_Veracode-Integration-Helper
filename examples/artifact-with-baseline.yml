name: Security Scan (Artefato/scan_file + Baseline Bantuu)

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build / package scan file (app.zip)
        run: |
          set -euo pipefail
          sudo apt-get update -y
          sudo apt-get install -y zip
          rm -f app.zip
          zip -rq app.zip . -x ".git/*" ".github/*" "node_modules/*" "*/node_modules/*"

      - name: Upload scan file
        uses: actions/upload-artifact@v4
        with:
          name: veracode-scan-file
          path: app.zip
          if-no-files-found: error

  security:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download scan file
        uses: actions/download-artifact@v4
        with:
          name: veracode-scan-file
          path: veracode-artifact

      - name: Veracode Integration Helper
        uses: JuanCunhaa/Veracode-Integration-Helper@v1
        with:
          bantuu_api_key: ${{ secrets.BANTUU_API_KEY }}
          veracode_api_id: ${{ secrets.VERACODE_API_ID }}
          veracode_api_key: ${{ secrets.VERACODE_API_KEY }}
          enable_auto_packager: 'false'
          scan_file: veracode-artifact/app.zip
          enable_baseline: 'true'
          policy_fail: 'false'

